#include "server.h"

// int main_loop(t_srv **srv)
// {
//     // FD_ZERO(srv->fd_read);
//     // FD_ZERO(srv->fd_write);
//     return 0;
// }


// void			add_fd(t_srv **srv)
// {
//   while (tmp != NULL)
//     {
//       if (tmp->type != FD_FREE)
// 	{
// 	  FD_SET(tmp->fd, &e->fd_read);
// 	  FD_SET(tmp->fd, &e->fd_write);
// 	  if (tmp->fd > e->fd_max)
// 	    e->fd_max = tmp->fd;
// 	}
//       tmp = tmp->next;
//     }
// }
